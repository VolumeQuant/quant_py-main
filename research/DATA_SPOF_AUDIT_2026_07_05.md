# 데이터 단일실패점(SPOF) 전수 점검 — 2026-07-05

> 목적: "각 데이터 소스/단계가 죽으면 무슨 일이 일어나는가"를 코드로 확인. 등급: 🟢 안전한 폴백 있음 / 🟡 기능 저하지만 무해 / 🔴 조치 필요.

| # | 의존성 | 실패 시 동작 (코드 확인) | 등급 |
|---|---|---|---|
| 1 | **KRX/pykrx — `_is_trading_day()`** | (구) 빈 응답 → "휴장일" 오판 → 무소음 결번 → ★2026-07-07 수정: 빈 응답+평일이면 yfinance KOSPI 교차확인 + 전 경로 개인봇 알림, 실패방향=진행 우선(결번>중복메시지). 4경로 모킹 테스트 PASS | 🟢 (수정됨) |
| 2 | **KRX/pykrx — kospi/kosdaq 지수 캐시** (`data_refresher` `'1001'/'2001'`) | (구) stale → 국면 MA 동결 → ★2026-07-07 수정: `run_daily._check_kospi_staleness` — 3영업일+ 뒤처짐/캐시 부재 시 개인봇 경보(비차단). 모킹 테스트 PASS | 🟢 (수정됨) |
| 3 | **krx_auth.login()** | (구) resp UnboundLocalError로 **send_telegram import 즉사 = 메시지 전멸** → ★2026-07-05 수정: resp=None 가드 + import시 try/except | 🟢 (수정됨) |
| 4 | 브레드스 계산 (`breadth_diagnostic`) | (구) 실패 → 빈문자열 = 조용히 사라짐 → ★2026-07-07 수정: 실패 시 "⚠️ 시장 참여폭 계산 실패 — 어제 상태 기준으로 판단하세요" 명시 라인(킬스위치 DISABLE만 빈문자열 유지). 모킹 테스트 PASS | 🟢 (수정됨) |
| 5 | DART (refresh_dart_cache) | timeout 5h 가드 + 실패 시 기존 캐시 진행 (run_daily 명시 처리) | 🟢 |
| 6 | FnGuide/WISEfn 컨센 (fusion) | 실패 → 전날 fusion_state 폴백 + 수집률 급감 경고(6/30 배포) + footer 빈문자열 | 🟢 |
| 7 | Gemini AI | 429/503 → graceful skip, 메시지는 발송 (6/18 검증) | 🟢 |
| 8 | Telegram API | 로컬 경로 `_send_batch` 메시지별 try (한 개 실패해도 계속). GHA 경로는 try 없음(현재 미사용) | 🟢 |
| 9 | ca_events.json | append-only (6/18 사고 후) + git 복구 가능 | 🟢 |
| 10 | ranking 산출 이상 | B 안전망: 종목수 150 미만 → 채널 차단·개인봇 알림·재시도 | 🟢 |
| 11 | 필터 조용한 사망 (env/입력오염) | ★2026-07-05 검문소 "필터 생존 모니터"(발동 종목수 0 → 경고) 배포 | 🟢 (신규) |
| 12 | 알파 자체의 부식 | ★2026-07-05 부식 경보(최근 60/120일 vs 역사 분포 하위 5%/15%) 배포 | 🟢 (신규) |
| 13 | 집PC 스케줄러 | 매일 즉사(리다이렉트 충돌) — 회사PC가 production이라 무해, 단 이중발송 방지 역할 겸중 → 비활성 권장(사용자 결정 대기) | 🟡 |

## 권고 (다음 주 반영 후보 — 이번 주는 배포 누적分 안착 우선)
1. **P1: `_is_trading_day` 빈 응답 처리** — `df.empty`인데 평일이면 ranking/market_cap 캐시 날짜로 교차확인 후 진행(또는 개인봇 경고 후 진행). "무소음 신호 결번"이 가장 위험한 실패 모드.
2. **P2: kospi 캐시 staleness 경보** — 캐시 최신일이 3거래일+ 뒤처지면 개인봇 알림 (국면 동결 감지).
3. **P2: 브레드스 실패 명시화** — 빈문자열 대신 "⚠️ 브레드스 계산 실패 — 전일 상태 유지 가정" 한 줄.

(모두 표시/경보 계층 — 매매 로직 무변경. 이번 주말 배포분: v80.33 X5 + v80.34 쿨다운 + 검문소 + FG 진단필드 + krx_auth 수정 — 월요일 첫 라이브 검수 후 다음 배치 진행 권장.)
