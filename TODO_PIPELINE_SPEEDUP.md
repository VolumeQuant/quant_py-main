# 파이프라인 실행시간 단축 분석 (v77, 2026-04-10)

## 현재 소요시간 (20260409 실측)

| 단계 | 소요시간 |
|------|---------|
| DART 증분 갱신 | 5분 05초 |
| data_refresher | 0분 14초 |
| FG boost (프리로드) | 4분 17초 |
| FG boost (스코어링) | 0분 01초 |
| FG defense (프리로드) | 3분 23초 |
| FG defense (스코어링) | 0분 01초 |
| 후처리+텔레그램 | 1분 11초 |
| git push | 0분 02초 |
| **합계** | **14분 14초** |

## 병목 분석

- **FG 프리로드 x2**: 7분 40초 (전체 54%)
  - FS parquet 4,311개 로드+merge가 핵심 (170초/회)
  - Market cap 1,546개, Fund 1,550개 → 단일일 런에 불필요한 전량 로드
  - merge_fs_supplement()의 iterrows() → 1,046,532건 순회
- **DART 갱신**: 5분 (전체 36%)
  - 906건 API × 0.15초 sleep + 처리시간

## 최적화 제안 (영향 순)

### 1. FG boost+defense 병렬 실행 (예상 -3분 30초)

현재: 순차 subprocess → 프리로드 2회 반복
제안: ProcessPoolExecutor로 동시 실행

- 출력: state/ vs state/defense/ → 파일 충돌 없음
- 환경변수만 다름 (가중치, 모멘텀기간)
- 메모리: ~4GB × 2 = 8GB 필요 (16GB RAM이면 OK)
- 위험: 없음 (결과 동일, I/O 분리됨)
- ⏱ 7분 40초 → ~4분 10초

### 2. 프리로드 데이터 공유 (추가 -2분 30초)

현재: 동일 OHLCV/FS/MC를 2회 로드
제안A: 인프로세스 호출 → preload 1회, 환경변수만 교체해 scoring 2회
제안B: 중간 캐시 (pickle) → 프리로드 결과 저장, 2번째 실행 시 로드 (20초)

- FS 로드(170초)가 핵심 → merge_fs_supplement의 iterrows() 최적화 가능
- 위험: 낮음. 환경변수가 scoring에만 영향, 프리로드는 동일
- ⏱ 7분 40초 → ~4분 30초 (1번과 조합 시 ~2분 30초)

### 3. 프로덕션 전용 경량 프리로드 (추가 -2분)

현재: BT용 설계 — MC 1,546일분 전체 로드
제안: 프로덕션 모드에서 최근 25일분만 로드

- avg_volume(20일) + find_nearest_cache에 충분
- Growth/TTM: trading_dates=1일이면 이미 최적
- FS merge: 유니버스 ~300종목만 로드 (2,767 → 300)
- 위험: 없음 (프로덕션은 당일 데이터만 필요)
- ⏱ 프리로드 4분 10초 → ~40초

## 조합 효과

| 시나리오 | 소요시간 | 단축 |
|----------|---------|------|
| 현재 | 14분 14초 | - |
| 3번만 | ~8분 | 44% |
| 1+3번 | ~6분 30초 | 54% |
| 1+2+3 | ~5분 30초 | 61% |

## 구현 우선순위

1. **3번 (경량 프리로드)** — 가장 안전, 큰 효과
2. **1번 (병렬)** — 3번과 조합 시 최대 효과
3. **2번 (공유)** — 1+3 조합으로 이미 충분하면 불필요

## DART 추가 제안

- **needs_refresh 빈데이터 스킵**: CFS+OFS 모두 empty면 바로 다음 분기로 → API 호출 30-40% 절감
- **fetch_single에서 2년→1년**: 비공시시즌엔 최신년도만 수집

---

## 별도 검토 과제: score 기반 wr

현재: wr = cr_t0×0.5 + cr_t1×0.3 + cr_t2×0.2 (rank 가중)
제안: wr = score_t0×0.5 + score_t1×0.3 + score_t2×0.2 (score 가중)

- 장점: score와 rank 정렬이 항상 일치 (역전 원천 불가)
- 리스크: Calmar 변화 가능 — **BT 검증 필수**
- 현재 wr 기반 점수 표시로 역전 문제는 우회 해결됨
- TurboSimulator에서 score 가중 wr vs rank 가중 wr 비교 BT 실행 필요
