# v80.8 인계 — bi 양방향 식 + 매매조건 정밀 그리드

> 2026-05-16 자율 작업. 사용자 부재 8시간 동안 진행. 회사 PC `git pull`로 동기화.

## TL;DR

사용자 지적("선익시스템·동아엘텍 같은 일시 계절성 종목에 매몰되지 않게 도와줘") 받고 v80.7→v80.8 업그레이드:
1. **계절성 식 curr → bi (양방향)** + PENALTY 0.5 → 0.3
2. **매매조건 9 axis 정밀 그리드** (entry/exit/slots/SL/TS/ts_cd)
3. **자동 누락 감지 추가** (5/16 사용자 발견 의존 사고 재발 방지)

7.4y BT 성과: **Cal 1.863 → 3.494 (+87%)**, CAGR +30.6%p, MDD -12.4%p

## 변경 사항

### 코드
1. `regime_indicator.py:get_regime_params`
   - boost: ENTRY_RANK 2 → **3**, TS_COOLDOWN 2 → **1**
   - defense: ENTRY_RANK 3 → **5**, EXIT_RANK 6 → **4**, TS_COOLDOWN 2 → **1**
2. `backtest/fast_generate_rankings_v2.py`
   - `SEASONALITY_FORMULA` default `curr` → **`bi`**
   - `SEASONALITY_PENALTY` default `0.5` → **`0.3`**
3. `backtest/regenerate_all_v80.py`
   - BOOST_ENV에 SEASONALITY_* 환경변수 추가
4. `send_telegram_auto.py`, `send_notice_once.py`
   - 매매 문구: 상위 2종목 → 3종목 (boost), 상위 3 → 5/매도 6위 → 4위 (defense)
5. `refresh_dart_cache.py`
   - **자동 누락 감지 + 개인봇 알림** (list API 종목 vs 캐시 대조)

### state
- v80.8 boost 1932일 재생성 (defense 무변경)
- 백업: `state_v80_7_quick_backup/` (mv 빠른 백업)
- git history: commit dc8378325 (v80.7), 69cd3ffe0 (5/15 ranking)

### 문서
- `CLAUDE.md`: v80.8 섹션 추가 (BT 그리드 결과 + 메커니즘)
- `memory/project_v808_bi_grid.md`: 신규
- `MEMORY.md`: 인덱스 + 버전 이력 갱신

## 그리드서치 (Phase 1~3)

### Phase 1 — 식 × 임계 × PENALTY (55 시나리오)
| 시나리오 | Cal | 비고 |
|---|---|---|
| **bi 1.4 / 0.3** | **2.610** 🏆 | 채택 |
| bi 1.4 / 0.4 | 2.568 | |
| bi 1.4 / 0.5 | 2.523 | 안전 절충 |
| curr 1.4 / 0.5 (v80.7) | 2.287 | 비교 기준 |
| top4_bot4 | 1.21~1.67 | 광범위 망함 |
| max2min2 / cv only | 1.22~1.53 | 망함 |

### Phase 2 — defense + 이격도
- defense 계절성 패널티 → **미적용** (모든 시나리오 baseline -0.005~-0.082)
- 이격도20 → **1.5 유지** (BT 영향 0)

### Phase 3 — 매매조건 9 axis
- entry_o 2→**3** (+0.315) ★
- entry_d 3→**5** (+0.087)
- exit_d 6→**4** (+0.112)
- ts_cd 2→**1** (+0.370 단독 우월) ★ 강건성 PASS (5 variant)

## 강건성 검증
- **ts_cd=1 우월**: 5 variant 평균 Δ +0.267 ✅
- **WF CV 0.638** ⚠️ (2019 dip Cal 0.320 — 약세장 약점)
- **단일 종목 의존성** Top1 1.3% ✅

## bi 식 한계 (못 잡는 패턴)
- Q1+Q2 폭증, Q3+Q4 폭증, Q2+Q3 폭증, Q4+Q1 폭증 (인접 2분기)
- 대각 합과 비슷 → ratio ≈ 1
- top4_bot4가 잡지만 정상 종목까지 잘못 잡아 BT Cal 1.21~1.67로 망침
- **현재로선 핀포인트 정확도 우선** (광범위 식 모두 baseline 미달)

## DART 누락 자동 감지 (Phase 4.5 1단계)

5/16 동아엘텍/선익시스템 26Q1 누락이 사용자 발견 의존이었음. 시스템 약점 인지.

**즉시 적용**: refresh_dart_cache.py에 자동 누락 감지 추가
- list API 종목 → fs_dart 캐시에 해당 분기 데이터 있나 확인
- 누락 종목 발견 시 개인봇에 알림 (목록 + 사유)

**추후 작업 (별도 PR)**: document API 메인 전환
- finstate_all (XBRL 인덱싱) → document (raw XML) 메인 전환
- 표본 검증 필요 — XBRL ACONTEXT 패턴 (CFY/PFY, ConsolidatedMember 등) 정확 파싱
- 시간 부족으로 자동 누락 감지만 우선 적용

## 회사 PC 적용 절차

```bash
git pull origin main
# config.py Gemini API 키 동기화 (필요 시 — .gitignore 등록)
# 다음 평일 16시 자동 실행 시 v80.8 자동 적용
```

state 1932일 재생성된 boost ranking이 git에 포함되므로 별도 재생성 불필요.

## 위험 / 롤백
- **모니터링**: 5거래일 KOSPI 대비 알파 -3%p 이하, MDD -10% 초과
- **즉시 롤백** (state 재생성 X):
  ```bash
  export SEASONALITY_FORMULA=curr
  export SEASONALITY_PENALTY=0.5
  # regime_indicator.py 매매조건 v80.7 복원 (별도 commit)
  ```
- **영구 롤백**: `git revert <commit>` 또는 `git checkout dc8378325 -- state/`

## 변경 안 한 것 (보수)
- KP_MA250_8d 국면 규칙
- V/Q/G/M 가중치 (boost V15Q0G55M30 / defense V35Q15G15M35)
- G 서브팩터 비율 (boost 3f 0.4/0.4/0.2 / defense 2f 0.8/0.2)
- 모멘텀 기간 (boost 12m / defense 6m-1m)
- SL -10%, TS -8%, slots_o 5, slots_d 4
- 이격도20 1.5

## 향후 작업

1. **DART document API 메인 전환** (Phase 4.5 2단계)
2. **NAV 디스카운트 별도 트랙** (Phase 5 — SK스퀘어 등 산업지주사)
3. **WF 2019 약세장 보강** — 보호 메커니즘 추가 검토
4. **인접 2분기 폭증 패턴** — top4_bot4 + 정상 종목 보호 결합 식 연구

## 자율 작업 작업 시간
- Phase 1 (EDA + Round 1+2): ~50분
- Phase 2 (defense + 이격도): ~5분
- Phase 3 (9 axis 그리드): ~80초
- Phase 3 강건성 검증: ~70초
- v80.8 코드 변경: ~10분
- v80.8 state 재생성: 백그라운드 진행 중 (~38분)
- 문서 + 자동 누락 감지: ~15분
