# 작업 원칙

## 사용자 지시 준수
- 사용자 지시와 다른 판단을 하려면 **반드시 먼저 물어볼 것**. 임의로 건너뛰지 마라.
- "대충 맞겠지"로 넘기지 말고, 확인 가능한 건 확인하고 진행
- 효율성을 위한 판단이라도 사용자 승인 없이 지시를 무시하면 안 됨

## 실행 전 검증
- 실행 전에 기본 가정을 확인하라 (날짜, 경로, 데이터 존재 여부 등)
- 같은 실수 2번 이상 반복하지 마라
- 각 단계 끝날 때마다 결과 검증 후 다음 단계 진행

## 병렬 실행 최적화
- 병렬 작업 전: CPU 코어 수, 가용 메모리, 프로세스당 메모리 확인
- 최적 병렬 수 = min(CPU코어, 가용메모리 ÷ 프로세스당 메모리)
- 근거 없이 임의 숫자(3개, 4개) 사용 금지

## 재사용 우선
- 변수 하나만 바꿔서 비교할 때, 전체 파이프라인을 처음부터 돌리지 마라
- 변하지 않는 부분을 재사용하라 (캐시, 이전 결과 등)
- 전체 재실행은 최후의 수단

## 성능 최적화
- 벤치마크 먼저, 병렬은 2~3개 최적 (pykrx는 절대 병렬 금지)
- pykrx: 1초 sleep, 순차 실행. 집 IP 이미 차단됨

---

# v70 전략 (2026-03-25 확정)

## 팩터 가중치
- **V20 + Q20 + G30 + M30** (g_rev=0.7: 매출70% + 영업이익30%)
- Value: PER + PBR + PCR + PSR (전체 유니버스 rank z-score)
- Quality: ROE + GPA + CFO (전체 유니버스 rank z-score)
- Growth: 매출성장률 × 0.7 + 영업이익변화율 × 0.3 (전체 유니버스 rank z-score)
- Momentum: 6M수익률/변동성 + K_ratio (**섹터 내** rank z-score)

## 진입/퇴출
- 진입: ✅ 3일 검증 + weighted_rank ≤ 5 (최대 7종목)
- 퇴출: weighted_rank > 8
- 손절: 진입가 대비 -10% (쿨다운 없음)
- 트레일링 스톱: 최고가 대비 -20% (수동, 시스템 미추적)

## 유니버스 필터 (변경 불가 — 백테스트 검증 완료)
- 시총 ≥ 1000억 (올리면 성과 폭락)
- 거래대금: 대형(1조+) ≥ 50억, 중소형 ≥ 20억 (올리면 성과 폭락)
- 턴어라운드 필터: 적용 안 함 (3종류 모두 성과 하락)
- 고점 필터(RSI/MA60): 적용 안 함 (전부 역효과)

## 검증 완료 사항
- 모멘텀 기간: 6M/Vol 압도적 1등 (85.2% vs 2등 53.5%)
- G팩터 비율: g_rev=0.7 최적 (0.65~0.75 모두 하락)
- Floor: 4팩터 중 1개라도 -1.5σ → 제외 (수익률 영향 없지만 품질 유지)
- 쿨다운: 없음 (MDD 개선 미미, CAGR 손실)

## 메시지 구조
- Signal: 시스템 수익률 → 상관관계 경고(0.65) → 매수 후보
- Watchlist: 매도 검토선 15번째 밑 (표시 순서 기준, weighted_rank 값 아님)
- 날짜: 항상 전일 기준 (d < today_str)
- 매매 조건 하단 표시

## 시스템 수익률
- OHLCV 기반 가격 (ranking JSON 아닌 all_ohlcv)
- 진입/이탈/슬롯/손절 규칙 동일 적용
- 복리 계산, 미실현 포함

## 스케줄러
- 일일 파이프라인: 매일 06:00 (QuanT_DailyPipeline)
- 종목명 캐시: 매주 월요일 09:00 (QuanT_TickerRefresh)
- DART 갱신: 매주 금요일 22:00 (QuanT_DartRefresh)

## 주의사항
- 프로덕션 state/ ranking 절대 재계산 금지 (FnGuide Growth가 rev_z/oca_z로 대체 불가)
- OHLCV 파일: sorted()[-1]이 아닌 명시적 파일명 사용 (2019 vs 2020 파일 혼동 방지)
- bt_2020~2025는 v70 가중치로 재계산 완료 (개별 팩터 z-score 변경 없음)
